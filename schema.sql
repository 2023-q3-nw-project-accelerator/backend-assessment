CREATE TABLE users(
    id SERIAL,
    age INTEGER,
    name VARCHAR,
    company VARCHAR,
    country VARCHAR,
    photo VARCHAR,
    about VARCHAR,
    latitude FLOAT(6),
    longitude FLOAT(6),
);


